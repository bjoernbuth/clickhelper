# clickhelper

Small package to provide a function to recursively generate help for click commands.

Example:

Suppose we have a small package super_calc meant for learning how to gropu click
commands. We created an abbreviation suca to reach the entry point of the package.

- suca corresponds to the main group
- there are a few subgroups for trying different things:
